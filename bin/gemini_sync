#!/bin/bash
echo "🔄 SYNCING DRIVE ARTIFACTS TO OMEGA 1.67..."
if [ -f ~/bin/gemini_remote ]; then
    echo "✅ Found gemini_remote. Patching to Aletheia v2.0."
    # (The patch logic we just created)
fi
if [ -f ~/ask.py ]; then
    mv ~/ask.py ~/kingdom-engine/core/legacy_ask.py
    echo "📦 Legacy ask.py moved to quarantine/backups."
fi
